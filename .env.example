# Optional URL of the deployed Python/Strands service, without a trailing slash.
# The web demo keeps working with its local safe fallback when this is empty.
NUTRIAHORRO_AGENT_URL=
NUTRIAHORRO_AGENT_TOKEN=
